/** Placeholder for stub bodies; every call site is replaced during implementation. */
export function todo(owner: string, what: string): never {
  throw new Error(`[${owner}] not implemented: ${what}`);
}
